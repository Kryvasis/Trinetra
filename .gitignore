# Compiled Java output
out/

# C++ build artifacts
Iskabon/build/
Iskabon/iskabon
Iskabon/iskabon_debug

# Object files / binaries
*.o
*.class
*.so
*.a

# Runtime scan/session output (regenerated per run)
Iskabon/session/
session/
trinetra_audit.db*

# Derived compliance score reports (per-session artifacts)
sessions/*/compliance_score_*.json
# Derived LLM narrative reports (per-session artifacts)
sessions/*/compliance_narrative_*.md
# Derived final audit reports (per-session artifacts, Prompt 17)
sessions/*/report_*.md
sessions/*/audit_report_*.md

# Local backups (not version-controlled)
*.bak_*
backup_*/
stat_scripts_backup_*/

# Temp files
*.tmp
*.lock
.DS_Store

# Local API keys (never commit)
config.json
