# Compiled Java output
out/

# C++ build artifacts
Iskabon/build/
Iskabon/iskabon
Iskabon/iskabon_debug

# Object files / binaries
*.o
*.class
*.so
*.a

# Runtime scan/session output (regenerated per run)
Iskabon/session/
session/
trinetra_audit.db*

# Derived compliance score reports (per-session artifacts)
sessions/*/compliance_score_*.json
# Derived LLM narrative reports (per-session artifacts)
sessions/*/compliance_narrative_*.md
# Derived final audit reports (per-session artifacts, Prompt 17)
sessions/*/report_*.md
sessions/*/audit_report_*.md

# Local backups (not version-controlled)
*.bak_*
backup_*/
stat_scripts_backup_*/

# Temp files
*.tmp
*.lock
.DS_Store
__pycache__/
*.pyc
*.pyo

# Local API keys (never commit)
config.json

# Bridge test sessions (ephemeral, created by pytest/flask bridge)
sessions/test_*/
sessions/demo_bridge_*/
sessions/consistency_*/
sessions/err_test_*/
sessions/bridge_test_*/
sessions/nonexistent_*/
sessions/this_session_*/
sessions/tmp_*/
sessions/live_fetch_*/
# Audit/demo ephemeral sessions (created by live audit scripts / manual demo runs)
sessions/audit_*/
sessions/audit_d*/
sessions/pdf_quick_*/
sessions/demo-judge*/
sessions/demo_judge*/
sessions/test_audit_*/
sessions/test_report_*/
sessions/test_severity_*/
sessions/test_devices_*/
sessions/fail_remediation_*/
sessions/train_loop_*/

# React frontend build artifacts
frontend/node_modules/
frontend/dist/
