! FortiGate FortiOS 7.2 — Trinetra Compliance Scanner Demo
! Device: fortigate-lab-01
! Vendor: Fortinet FortiGate
! OS: FortiOS 7.2.4 (header for OS-version detection demo)
config system global
    set admintimeout 5
    set hostname fortigate-lab-01
end
config system interface
    edit port1
        set ip 10.10.3.1 255.255.255.0
        set allowaccess ping https ssh
    next
    edit port2
        set ip 192.168.3.1 255.255.255.0
        set allowaccess ping https
    next
end
config system admin
    edit admin
        set password ENC SH2Fak123456789
    next
end
config system snmp community
    edit 1
        set name public
        set status enable
        set hosts 0.0.0.0 0.0.0.0
    next
end
config log syslogd setting
    set status enable
    set server 10.10.1.100
    set port 514
end
! --- UNRECOGNIZED LINE (for training demo) ---
config system fortiguard
    set custom-feature enable zone-trust
end
